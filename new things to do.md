it should check if there's a template linked to an assignment before running compare all. if there is a template then create a fingerprint and then subtract it before running any comparisons.

strip all comments

